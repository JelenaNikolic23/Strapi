import React, { Component } from "react";

export class Kategorije extends Component {
  render(props) {
    //const { details } = this.props;

    // const geropa = details.map((cat, ind) => {
    //   return <h1>{cat.categorySteps.stepName}</h1>;
    // });
    return <div>{}</div>;
  }
}

export default Kategorije;
